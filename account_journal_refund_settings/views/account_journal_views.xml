<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <record id="view_account_journal_tree" model="ir.ui.view">
    <field name="name">account.journal.tree</field>
    <field name="model">account.journal</field>
    <field name="inherit_id" ref="account.view_account_journal_tree"/>
    <field name="arch" type="xml">
      <field name="type" position="after">
        <field name="refund_usage" invisible="type not in ['sale','purchase']"/>
      </field>
    </field>
  </record>

  <record id="view_account_journal_form" model="ir.ui.view">
    <field name="name">account.journal.form.refund</field>
    <field name="model">account.journal</field>
    <field name="inherit_id" ref="account.view_account_journal_form"/>
    <field name="arch" type="xml">
      <field name="refund_sequence" position="attributes">
        <attribute name="invisible">
            type not in ['sale','purchase'] or refund_usage == 'regular'
        </attribute>
      </field>
      <field name="currency_id" position="after">
        <field name="refund_usage"
               invisible="type not in ['sale','purchase']" />
        <field name="refund_journal_id"
               invisible="type not in ['sale','purchase'] or refund_usage != 'regular'" />
      </field>
    </field>
  </record>

  <!-- TODO - DASHBOARD KANBAN VIEW -->

</odoo>
